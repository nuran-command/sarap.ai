services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: sarap
      POSTGRES_PASSWORD: sarap
      POSTGRES_DB: sarap
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

